[package]
name = "fastgui-chrome"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
fastgui-core = { path = "../fastgui-core" }
cosmic-text = "0.14"
tiny-skia = "0.11"

[features]
# `testing::check_gpu_backend`, the shared scenario backend GPU tests draw and compare.
testing = []
